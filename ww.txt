# Remove these files:
del run_sitemap_generation.py
del run_daily_sitemaps.ps1  
del scripts/render_template_check.py
del validate_production_sitemaps.py

# This file is no longer used:
storefront/views/sitemap_static.py

# Remove directory (Git will clean this up on next commit):
rm -r storefront/static/sitemaps/

# Remove sitemaps from future commits (won't affect current files):
git rm --cached storefront/static/sitemaps/ -r
git commit -m "Remove pre-generated sitemaps (now dynamic)"